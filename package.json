{
  "name": "brisk.css",
  "version": "0.0.1",
  "description": "CSS Framwork",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "sass --watch ./src/brisk.scss ./dist/brisk.css",
    "min": "sass --watch ./src/brisk.scss ./dist/brisk.min.css --style=compressed"
  },
  "keywords": [
    "css",
    "scss",
    "framework",
    "utilities",
    "grid"
  ],
  "author": "Tom Deser",
  "license": "ISC",
  "devDependencies": {
    "sass": "^1.26.3"
  }
}
