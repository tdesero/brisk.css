{
  "name": "brisk.css",
  "version": "0.0.1",
  "description": "CSS Framwork",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "sass --watch ./src/brisk.scss ./dist/brisk.css",
    "min": "sass --watch ./src/brisk.scss ./dist/brisk.min.css --style=compressed",
    "docs-pug": "pug --watch ./docs/src/pug -o ./docs/",
    "docs-sass": "sass --watch ./docs/src/scss/style.scss ./docs/dist/style.css"
  },
  "keywords": [
    "css",
    "scss",
    "framework",
    "utilities",
    "grid"
  ],
  "author": "Tom Deser",
  "license": "ISC",
  "devDependencies": {
    "jstransformer-highlight": "^2.0.0",
    "pug": "^2.0.4",
    "sass": "^1.26.3"
  },
  "dependencies": {
    "pug-cli": "^1.0.0-alpha6"
  }
}
